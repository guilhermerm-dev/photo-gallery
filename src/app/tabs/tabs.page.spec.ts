import { TabsPage } from "./tabs.page";

describe('TabsPage', () => {
    let component: TabsPage;

    it('Should create component', () => {
        component = new TabsPage();
        expect(component).toBeTruthy();
    })
});
